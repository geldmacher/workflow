Do not author `delivery-evidence` directly. In Cursor Manual, fresh Review calls `workflow_closeout` once with `artifact_kind: work-review`, fresh Check observations, and closed review input; a protected single-use host receipt supplies the exact approved native Root and validated current-task predecessor bytes. Codex and portable clients retain the exact Root/artifact input path. The server derives the complete repository snapshot and dirty inventory, enforces Root authority, and returns paired Evidence and Review or neither.

The builder owns identity, hashes, topology, representation, evidence mode, aggregate grade, status, and rendering. Caller paths and snapshots are hints only. Manual verified Checks require direct fresh reviewer observation of the planned method, directory, expectation, result, and repetition. Missing proof downgrades; known failures stay failed.

Lead with outcome, Checks, limitations, and one next action. Keep exact artifacts in structured output and reference their IDs in technical traceability. `handoff_persisted: false` is expected for task-local Review and is not a blocker. Never claim live host activation or production success from repository evidence.

Cursor output identifies `native_task_binding: cursor-receipt`, `native_root_source`, additive `native_root_binding`, and `predecessor_mode: task-chain|full-rebuild`; full rebuild never claims missing delta lineage. Enforced post-tool or transcript observation uses `cursor-create-plan`. The unique recent native Plan-file fallback uses `cursor-plan-file`, remains provisional, and can never yield verified delivery. Portable clients may still call delivery-evidence mode and use content-addressed handoff under the compatibility contract. That transport cannot restore Cursor or Codex Manual authority.

See the [Manual Workflow guide](https://github.com/geldmacher/workflow/blob/main/docs/manual-workflow.md#fresh-review-and-atomic-evidence) for the native review-owned Evidence boundary.
