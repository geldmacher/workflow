Do not author `delivery-evidence` directly. For native Manual closeout, supply one strict `closeout-input`; omit `changed_paths` when possible because any supplied list is only a compatibility hint. The host independently resolves exact Root/chain text, derives the complete changed-path inventory and repository snapshot, enforces Authority, and binds profile and lineage before calling the deterministic builder. `workflow_closeout` remains an optional compatible transport. The builder owns identity, hashes, topology, representation, evidence mode, aggregate grade, status, and rendering.

Lead with outcome, checks, gaps, and `### Next step`. Keep the exact generated artifact in `structuredContent.artifact` and attach it explicitly when `handoff_persisted` is false; do not dump the full artifact into chat when it is already retained. A true cache-write failure leaves an explicitly rooted artifact valid and requires the returned attach instruction. The result reports `workspace_binding`, `workspace_root_used`, `handoff_persisted`, `handoff_mode`, `root_content_hash`, and `handoff_error_code`. Content-bound handoff persists under the exact Root text hash even when MCP `roots/list` is unavailable; then `handoff_mode` is `root-content-cache` and `workspace_binding` remains `not-established` unless a host-configured or advertised workspace is established. A tool-supplied `workspace_root` alone never grants workspace authority. Missing exact Root input, an incomplete lineage, Root/hash mismatch, semantic conflict, or—when workspace authority is resolved—a foreign, redirected, drifting, or ambiguous Root emits no artifact. Never claim production success from repository evidence. See [manual MCP output](manual-mcp-output-contract.md).

When Evidence is `supported`, `partial`, `unavailable`, or `failed`, or when handoff needs explicit attachment, add one contextual `Meaning:` sentence and the matching [Manual Workflow guide](https://github.com/geldmacher/workflow/blob/main/docs/manual-workflow.md#evidence-grades) topic before the final action block.
