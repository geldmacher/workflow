Do not author `delivery-evidence` directly. Supply exact Root/chain text, changed paths, repository snapshot, effective profile, Strategy revision, and structured Check observations to `workflow_closeout`; the deterministic builder owns identity, hashes, topology, representation, evidence mode, aggregate grade, status, and rendering.

Return the generated artifact byte-for-byte. A true cache-write failure leaves an explicitly rooted artifact valid and requires the returned attach instruction. The result reports `workspace_binding`, `workspace_root_used`, `handoff_persisted`, `handoff_mode`, `root_content_hash`, and `handoff_error_code`. Content-bound handoff persists under the exact Root text hash even when MCP `roots/list` is unavailable; then `handoff_mode` is `root-content-cache` and `workspace_binding` remains `not-established` unless a host-configured or advertised workspace is established. A tool-supplied `workspace_root` alone never grants workspace authority. Missing exact Root input, an incomplete lineage, Root/hash mismatch, semantic conflict, or—when workspace authority is resolved—a foreign, redirected, drifting, or ambiguous Root emits no artifact. Never claim production success from repository evidence.
