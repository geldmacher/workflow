# Release validation record

Validation date: 2026-07-29. Target: Workflow 3.0.0 manual core plus Premium Auto-Planning and controller Shadow Mode. See [the release checklist](release-checklist.md).

| Check | Status | Evidence |
|---|---|---|
| Plugin structure and release metadata | Passed | `npm run release-check` completes generated parity, 156/156 deterministic tests, context targets/ratchet, release and private-Marketplace validation, and Markdown link validation. Plugin, package, and lock metadata remain at 3.0.0. |
| Breaking artifact schema 3 | Passed | Complete `oneshot`, `compact`, and `full` chains pass. Schema-2 artifacts and mixed chains fail before graph materialization. Required semantic fields, every Evidence Class, exact assurance, writer tier, correction Learning candidates, and evidence/review topology are never inferred or repaired. Closed artifacts reject unknown fields while opaque `extensions` remains valid and changes the raw artifact hash without changing the authoritative projection hash. |
| Manual schema-3 workflow | Deterministically passed; live Cursor smoke blocked | Plan → Implement Plan → Review → Correct → Learn → Explain remains human-gated and schema-3-only. Review explicitly starts from a fresh Ask context without Writer assumptions, and manual consumers resolve topology by the semantic `artifact` field rather than filenames. Manual skills and auditors treat extensions as opaque, exclude them from decisions and handoffs, and never interpret, quote, summarize, explain, or use their contents. Stateless `/work-status wp-*` projects exact task artifacts through the shared state graph without a model or persistent controller state; missing context waits, while invalid present chains replan. `/explain-work` rejects schema-2 and mixed chains. These checks prove prompt and contract presence, not equivalent behavior from a live model. |
| Design depth, slices, and explanation | Passed | Tests cover complete chains for `oneshot`, `compact`, and `full`, Product/Architecture/Program Design/Slice contracts, automation bounds, and `/explain-work` for missing, active, correctable, and achieved roots. The explanation remains read-only, chat-only, and non-evidentiary. |
| Derived workflow state | Passed | Table tests cover all public states, skipped lean design nodes, profile-sensitive actors/actions, manual Plan/Correction gates, post-Correction Review, missing versus invalid artifact chains, downgrade, interruption, auto-gated final acceptance, and unattended local completion. No repository session-state artifact exists. |
| Premium Auto-Planning | Passed with mock Worker; live paid smoke not run | `workflow_prepare` accepts one goal or valid schema-3 Root, rejects schema 2/incomplete schema 3 before the Planner, captures exactly one Plan or Intent-blocker report, uses same-Agent bounded validator repair, computes deterministic semantic diffs, and creates no Run. Existing-root Planning receives only the extension-free projection and restores only the original opaque extensions; goal Planning strips invented extensions. Tests cover missing budgets, remaps, token exhaustion, repair limits, Planner locking, read-only/secret boundaries, expiry-state semantics, and unchanged/improved Roots. |
| Controller contracts | Passed | New Preparations and Runs freeze record schema 1, artifact schema 3, controller protocol 3, plugin 3.0.0, the raw Root hash, and a separate authoritative-projection hash. Preparation idempotency is bound to source kind, Goal or raw Root hash, requested profile, and Route profile; mismatched reuse fails and invalid Roots are rejected before duplicate resolution. Planner receipts bind input and normalized output projections; Writer and Reviewer prompts and receipts bind the frozen Run projection. Hash-bound `workflow_start` accepts only a compatible, unexpired `root-ready` Preparation with unchanged baseline/route/config/policy/harness and complete Planner receipts, then atomically binds one consumed Preparation to one `plan_approved` Run. Exact five-role routing, closed configuration/policy, `fallback: deny`, budgets, immutable safety precedence, revisions, idempotency, locks, external state, Worktrees, checkpoints, direct-argv host checks, and fail-closed authorization are covered. |
| Historic external runs | Passed | Protocol-incompatible runs are derived as stopped/read-only with `incompatible-run-protocol`, remain status/watch-visible and byte-identical, reject mutations/resume/runner use, do not block a new run, and never qualify as history. |
| Auto-gated loop with mock worker | Passed | Writer owns implementation and corrections; Writer and fresh Review receive the frozen authoritative projection without raw extensions or Writer conversation; every phase receipt must match its hash. A repeated Finding causes one irreversible escalation at the Review-to-Correction boundary; successful delivery stops on the local run branch. Out-of-scope writes, projection mismatch, and unknown dependency drift stop. |
| Runtime bundles | Source implemented; rebuilt parity pending final gate | MCP/controller stays bundled and the SDK stays external. The explicit provisioner stages the exact lockfile tree outside the plugin, atomically publishes a hash manifest, and refuses overwrite. Only a Plugin-hash-matching provisioned Worker is automation-eligible; local development, `GELDMACHER_WORKFLOW_WORKER`, and explicit test entrypoints remain Shadow-only. |
| Capability spike | Negative by design; activation denied | Capability Receipt schema 2, exact hash/version/route/harness/audit binding, 30-day expiry, logical-AND derivation, cooperative cancel evidence, three-run live canary contracts, cost gate, and atomic issuance are implemented. The negative `--issue-receipt --require-automation-safe` run exits 1: local seven-tool MCP and state/worktree/restart pass; Marketplace, live catalog, paid model/Planner/boundary/cancel, crash, and Cursor harness remain absent; nested outer sandbox fails. Result remains `automation_safe: false`; `receipt_issued: false`. |
| Marketplace package | RC manifest implemented; installation not certified | `.cursor-plugin/marketplace.json` exposes exactly `geldmacher-workflow` with `source: "."` and is checked against the vendored official schema. The exact-commit private Marketplace add/install and the installed-copy MCP/provisioner smoke remain external certification steps. |
| Model availability and paid SDK smoke | Not run | Live checks require valid environment-only `CURSOR_API_KEY`; inference-bearing create/resume additionally requires explicit cost approval. No exact live model availability or model attestation is claimed. |
| Dependency audit | Failed activation gate; waiting upstream | The expressly authorized `npm audit --omit=dev --json` on 2026-07-29 reports three affected production packages: High `undici`, Moderate `@cursor/sdk`, and Moderate `@connectrpc/connect-node`; all report no available fix. The lock contains 137 entries including Root, 136 resolved packages. The report is externally archived with lockfile hash at `~/.cursor/geldmacher-workflow/state/e8a91656bb74d02e6a13/certification/2026-07-29T12-34-35-675Z/npm-audit.json`; audit evidence hash is `de51352914edfc9e2b6c852cd968aeca6711c7e39190fa0c21f64835c6e7f314`. High blocks any positive Receipt or writable Auto activation. The transport-path Moderate findings require separate human risk acceptance after High is fixed. No override, force, or audit-fix is used. |
| Cursor SDK redistribution | Not used; future form requires clearance | The installed SDK declares an all-rights-reserved license subject to Cursor Terms. The plugin does not copy it into `dist`. Technical packaging must not be represented as redistribution permission. |
| Context economy and ratchet | Passed | Always-on context is 0 and discoverability is 326. Manual phase paths are Plan intake 648, oneshot Plan 1863, Correction 2000, base Review 1987, Learning 1809, and Explanation 1157. Compact/full Planning is 2252 and Correction Review is 2459; the latter loads Correction authoring but not Learning closeout. Automation paths are Prepare/Approve 1214, Status 909, Watch 890, Control 1499, and Models 853. Every selected target passes, per-file breakdowns match their flow totals, direct Command/Skill contract links match the measured load graph, and the versioned checked baseline rejects context growth or load-graph drift while accepting reductions. |
| Cursor Editor/CLI harness | Blocked for 3.0.0 | `/private/tmp/cursor-plugin-harness` is absent. Cursor Agent 2026.07.23-e383d2b is installed, but both `status` and `--list-models` terminate with `SecItemCopyMatching failed -50` and signal 11. Native Question UI and all manual schema-3 Command smokes therefore remain unverified; prior 2.x evidence is not promoted. |

The implemented result is the breaking schema-3 manual workflow, `/explain-work`, Premium Auto-Planning/Preparation contracts, and a fail-closed controller Shadow Mode with the negative-certification machinery required for Point 5. Operational certification remains blocked by the current High dependency finding, missing valid `CURSOR_API_KEY` environment, Keychain crash, absent installed RC path, and missing live harness/probe/history evidence. `auto-gated` and `unattended-eligible` are not activated until every hard observation is positive for the exact installed Plugin, SDK, platform, routes, and harness.
