# Release plugin

Read and follow [the shared release skill](../../.agents/skills/release-plugin/SKILL.md). This explicit command grants the same authority: choose the appropriate semantic version from actual changes, prepare consistent versions and notes, then run `npm run release:plugin` to validate and publish in one invocation. Preserve exact retry states and all publication safeguards described there.
