---
name: work-learning
description: Persist bounded confirmed learnings after verified Schema-6 delivery.
---

Use an implementation-capable host mode. Read [protocol](../../references/artifact-protocol.md) and [learning closeout](../../references/learning-contract.md) completely.

Invocation separately authorizes bounded project-guidance edits. Trailing text is Learning, never a selector; do not accept, integrate, publish, or mutate workflow state.

Resolve exact current-task Schema-6 Root/Evidence/Review bytes with an achieved verified result. Unsupported state, cache-only history, missing proof, ambiguity, provisional evidence, or failed Checks stops.

Trust candidates only after confirmed repository proof and separate human authorization. Add trailing text once, deduplicate, preserve unrelated work, and report result, destination, paths, and proof. Learning never expands Root or harness authority.
