---
name: work-review
description: Review one Schema-6 Workflow root repository-read-only.
---

Start fresh and repository-read-only. Read [protocol](../../references/artifact-protocol.md) and [review](../../references/review-contract.md); use the closed Manual schema.

Require exact task Root/predecessors; fresh tasks need attachment. Cache, hooks, IDs, transport, or ambiguity grant no authority. Before any repository inspection, run local `validate-plan` on the current native Plan or attached Root. The builder extracts the single embedded Root from a native container; stop immediately if validation does not return the expected Root ID. Reuse that unchanged Root input for Review.

Inspect every objective, boundary, Check, and limit. Separate outcome from proof; the harness owns inspection.

Supply closed Schema-1 Review, repository, and Check observations. Use `presentation_locale: de` only when the active request is German, else `en`. Partition every dirty path exactly once as Root-subject or ambient; uncertainty is subject. Grades are `supported|partial|unavailable|failed`; author no IDs, hashes, attestations, or verified claims. `supported` needs a conclusive current-snapshot outcome, never invocation, presence, masked status, or unknown outcome. Observe required Checks; optional Checks may be omitted. Reuse observations only on the same snapshot.

Only after Root validation, inspect and run local `build-review` once. It atomically returns presentation and artifacts without runtime state. Finding-free plus supported or verified required Checks is `achieved` with no action; supported stays non-verified. Ambient paths are visible and non-blocking. Subject scope drift is provisional; hard boundaries and failed required Checks block.

Render `human_output` once. Use the closed Cursor map; decorate only `presentation.next_action`: `correct`→`/correct-work`; `accept-provisional`→`/accept-work provisional`; `replan`→`/plan-work replan`; `retry-review|review-root`→`/review-work`; `clarify`→answer; `provide-artifacts`→exact chain; `none`→no action. No fallback; keep the token in trace. Add no assessment or postscript. Put each artifact once, unchanged and unquoted, in its own default-closed `<details>`. Shadow creates none. Never mutate.
