---
name: work-execution
description: Apply one approved implementation or correction inside Schema-6 Root authority.
---

Read [protocol](../../references/artifact-protocol.md) and [correction](../../references/correction-contract.md) completely.

Resolve the exact Root and, for correction, its newest unique actionable tip. Validate correction chains with local `status`; initial implementation needs only the approved Root. Reject unsupported schemas, conflicts, stale lineage, or authority drift.

Workflow availability never gates ordinary host use. Stay inside the native Plan; changed intent, scope, risk, protected paths, dependencies, or effects requires an approved replan.

Apply required outcomes inside Root authority. The project harness owns every execution and verification choice; Workflow judges none.

Create no Evidence or Workflow state. Report outcomes and limitations, then request fresh `/review-work`. Observations stay unprotected until local Review or separate protected sealing.
