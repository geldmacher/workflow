---
name: plan-work
description: Create one free-form Workflow plan with a generated Authority Core in Plan Mode.
---

# /plan-work

Designed for Cursor Plan Mode. Accept `/plan-work <goal>` and read [work-planning](../skills/work-planning/SKILL.md) completely. When the human deliberately asks for a new plan from Open Points, resolve optional predecessor bindings only from exact current-task Schema-6 Root/Review bytes. Unsupported artifact schemas are rejected.

Invocation authorizes planning only. Pass the active German or English locale. Emit no Root before intent is ready; call local `build-plan` so the generated Authority Core binds the free-form plan. Implementation requires the human-selected **Implement Plan** action.
