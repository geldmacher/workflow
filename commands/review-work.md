---
name: review-work
description: Review one Workflow root read-only.
---

# /review-work

Designed for Cursor Ask Mode; fresh Review uses the exact Schema-5 Root from the current task's native Cursor Plan. Read [work-review](../skills/work-review/SKILL.md) completely.

Read-only. Return closed Schema-1 input; host owns Review. Follow its action: `correct` → `/correct-work`, never `/work-status`/another Review. Do not execute correction or author/shorten `wr-*` bytes. Verified completes; provisional needs `/accept-work provisional`.
