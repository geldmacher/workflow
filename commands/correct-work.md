---
name: correct-work
description: Apply the latest actionable correction economically and return reusable evidence.
---

This command is designed for Cursor Agent Mode. Cursor defines which implementation and verification capabilities are available; a correction may be verification-only and require no repository edit.

Read and follow [work-execution](../skills/work-execution/SKILL.md) completely. `/correct-work` accepts no arguments; invocation approves the newest unique actionable review. Resolve its effective model, resume idempotently, return delta/full evidence, then hand back to Ask `/review-work`.
