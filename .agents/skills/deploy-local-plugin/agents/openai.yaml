interface:
  display_name: "Deploy Local Plugin"
  short_description: "Build and deploy this local plugin safely"
  default_prompt: "Use $deploy-local-plugin to deploy this repository to Cursor and Codex."
policy:
  allow_implicit_invocation: false
