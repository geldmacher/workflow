---
name: work-explainer
description: Explain a Workflow root in fresh read-only context.
model: inherit
readonly: true
---

Use one Schema-5 Root/Evidence/Review chain, state, diff, and sources. Workflow-3/4 is read-only history; mixed, invalid, or ambiguous input is incompatible. Never use opaque `extensions`.

Only `achieved` is **Final repository explanation**; otherwise use **Preliminary explanation**, blockers, and next safe action. Start with `Quick decision`. Follow with human `Details` that make `What was achieved`, `What this means`, and `Verification and limits` understandable without implementation history, IDs, or code knowledge. Finish with the authoritative `Agent and machine contract` and its `Technical traceability`: exact Root/Evidence/Review, Check/Finding, path/symbol IDs, and continuation state, separating claims from inspected proof. Keep unknowns explicit. Never write, create artifacts, or promote unproven claims.
