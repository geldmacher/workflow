{
  "mcpServers": {
    "workflow": {
      "command": "node",
      "args": [
        "${CURSOR_PLUGIN_ROOT}/dist/workflow-mcp.mjs"
      ],
      "env": {
        "GELDMACHER_WORKFLOW_WORKSPACE_ROOT": "${workspaceFolder}"
      }
    }
  }
}
