{
  "name": "geldmacher-workflow-plugin-development",
  "version": "2.1.0",
  "private": true,
  "description": "Development validation for the tolerant, adaptive-assurance Geldmacher Workflow Cursor plugin with learning closeout.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "validate": "node scripts/validate-plugin.mjs",
    "test": "node --test",
    "context-budget": "node scripts/measure-context.mjs --check",
    "link-check": "node scripts/check-markdown-links.mjs",
    "build:runtime-validator": "node scripts/build-runtime-validator.mjs",
    "check:runtime-validator": "node scripts/build-runtime-validator.mjs --check",
    "release-check": "npm run check:runtime-validator && npm test && npm run context-budget && node scripts/validate-plugin.mjs --release && node scripts/check-markdown-links.mjs",
    "validate-artifact": "node scripts/validate-artifact.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "esbuild": "^0.25.12",
    "yaml": "^2.8.1"
  }
}
